#include<stdio.h>

// Define the struct as card
// Declare the member of card are: face, suit.
struct card
{
	char *face;
	char *suit;
};

int main( void )
{
	struct card aCard;
	struct card *cardPtr;
};


